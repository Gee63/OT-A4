import { MobileMenuCloseDirective } from './mobile-menu-close.directive';

describe('MobileMenuCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileMenuCloseDirective();
    expect(directive).toBeTruthy();
  });
});
