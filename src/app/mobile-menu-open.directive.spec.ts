import { MobileMenuOpenDirective } from './mobile-menu-open.directive';

describe('MobileMenuOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileMenuOpenDirective();
    expect(directive).toBeTruthy();
  });
});
